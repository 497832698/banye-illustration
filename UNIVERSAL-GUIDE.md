# 般叶配图 · 非 WorkBuddy 通用使用指南

> 本 Skill 原生为 **WorkBuddy** 设计，但**不依赖 WorkBuddy 也能用**——它本质是一套
> 「角色锚点锁定 + 怪诞手绘风格 + 8 套换装参考图 + 提示词模板」。任意支持
> **图片参考 + 文本提示词**的 AI 生图工具都能复现同样效果。
>
> 本指南教你把 `assets/` 里的参考图和 `portable/` 里的提示词，用在常见 AI 工具上。

---

## 0. 你需要准备什么（都在仓库里）

| 文件 | 作用 | 路径 |
|------|------|------|
| **设定图（必传）** | 般叶全身身份锚：银白短发、左1右4银环、银项链、白西装、Q版大头 | `assets/ip/banye/reference-character.png` |
| **校准图（强烈建议）** | 般叶在怪诞手绘风里的样子，用来锁「线稿质感仍像同一人」 | `assets/ip/banye/examples/quirky-sketch/banye-sample.png` |
| **服装参考图（换装时传）** | 每套衣服的具体造型，按关键词选 | `assets/ip/banye/outfits/{id}/reference.png` |
| **多套穿搭总览（可选）** | 一次性看 8 套造型 | `assets/ip/banye/reference-outfits.png` |

8 套服装 `id` → 触发词：

| id | 服装 | 你说的话 |
|----|------|---------|
| `white_suit` | 白西装（默认） | 白西装 / 西装 / 不指定 |
| `short_sleeve` | 短袖 | 短袖 / 半袖 / T恤 |
| `vest` | 背心 | 背心 / 无袖 / 坎肩 |
| `black_suit` | 黑西装 | 黑西装 / 暗黑西装 |
| `tuxedo` | 礼服 | 礼服 / 正装 / 燕尾服 |
| `denim` | 牛仔 | 牛仔 / 丹宁 |
| `korean` | 韩系 | 韩系 / 清爽 |
| `visual` | 杀马特/视觉系 | 杀马特 / 视觉系 |

---

## 1. 核心原理：3 张参考图 + 1 段提示词

无论用哪个工具，逻辑都一样：

1. **传参考图**（图片）：
   - 图 1 = 设定图（管「是谁」：发色、耳环、项链、比例）
   - 图 2 = 校准图（管「画成什么手绘质感」）
   - 图 3 = 服装参考图（管「穿哪套」，不指定则用白西装设定图自带造型）
2. **贴提示词**（文本）：`portable/prompts-en.txt` 里对应那套的完整英文 prompt。
3. **出图**。

> 为什么要传图？因为般叶的「左耳 1 环 / 右耳 4 环」这种不对称细节，纯文字模型很容易画错。
> 传参考图能把认错人的概率降到最低。

**锚点（传图 + 提示词双重锁定，无论换什么衣服都不变）：**
- 银白普通短发（非长发、非金发）
- 左耳 **1** 枚银环 + 右耳 **4** 枚银环（不对称是核心识别点）
- 银色链条项链
- 不戴眼镜
- 黑色上挑眼线，痞帅冷感
- Q 版大头，头身比约 1:1.3（约 2–3 头身）

---

## 2. 各工具操作步骤

### A. ChatGPT（DALL·E）/ 通义万相 / 豆包 / 即梦 —— 最推荐，直接支持「图+文」

1. 新建对话，上传 **3 张图**（设定图 + 校准图 + 你要的服装参考图）
2. 把 `portable/prompts-en.txt` 里对应服装的那段英文 prompt 整段粘贴进去
3. 发送，等待出图
4. 不满意就让模型「保持参考图里的般叶形象，把 XX 改成 XX」微调

> 中文工具（通义/豆包/即梦）也可直接粘贴中文意图 + 上传图，但英文 prompt 更稳定，建议优先用 `prompts-en.txt`。

### B. Midjourney —— 用 `--cref` / `--sref`

Midjourney 不吃长文本 + 多图，改用它的参考参数：

1. 把 `reference-character.png` 和 `banye-sample.png` **上传到任意可外链图床**（或用 Discord 里 MJ 的图片上传），拿到图片 URL
2. 在 MJ  prompt 里写：

```
Banye, <服装英文描述>, quirky hand-drawn sketch, wobbly ink lines, white background, editorial doodle
--cref <设定图URL> --cw 100 --sref <校准图URL> --ar 16:9
```

- `--cref`：人物参考（锁般叶的脸/耳环/比例）
- `--cw 100`：让服装也跟着参考图（若你想完全用 MJ 自己生成衣服，可降到 `--cw 0` 再靠文字描述）
- `--sref`：风格参考（锁怪诞手绘质感）
- 服装描述用 `portable/prompts-en.txt` 里对应那套的 `Current outfit:` 句子

> MJ 对「左1右4耳环」这类细节还原有限，必要时多生成几张挑最像的。

### C. Stable Diffusion（WebUI / ComfyUI）—— 用 IP-Adapter

1. 安装 `IP-Adapter` 扩展
2. 把 **设定图** 作为 IP-Adapter 人脸/角色参考（weight ~0.8）
3. 把 **校准图** 作为风格参考（weight ~0.4）
4. 把 **服装参考图** 作为额外 IP-Adapter 参考（weight ~0.6，仅管衣服）
5. 正向提示词贴 `portable/prompts-en.txt` 对应段；负向提示词贴文件末尾的 Negative 段
6. 出图

### D. 纯文本工具（不支持图输入）—— 兜底方案

如果工具完全不能传图（如早期 DALL·E 2 文字版），直接把 `prompts-en.txt` 整段贴进去也能出图，
**但「左1右4耳环」等细节容易画错**，需多试几张并挑图。锚点已在提示词里用文字强调，尽力保真。

---

## 3. 提示词文件说明

`portable/` 目录：

| 文件 | 内容 |
|------|------|
| `base-prompt.txt` | 自包含基础 prompt（身份锁 + 风格 + 负向），含 `{OUTFIT}` 占位，可自己替换 |
| `prompts-en.txt` | **8 套服装的完整英文 prompt**，直接复制对应那套即可 |
| `prompts-zh.txt` | 中文说明 + 服装关键词映射，方便你用中文工具时对照 |

---

## 4. 常见翻车 & 排查

| 现象 | 原因 / 解决 |
|------|------------|
| 耳朵环数不对（如画成对称 2+2） | 没传设定图，或 MJ 类工具还原弱 → 传图 + 文字强调「LEFT 1 / RIGHT 4」 |
| 头发变长 / 变金 | 提示词里强调了「silver-white SHORT regular hair (NOT long)」；检查是否漏传设定图 |
| 戴了眼镜 | 提示词已写 `NO glasses`；若仍出现，在负向里再加 `glasses` |
| 背景太满 / 像 PPT | 提示词已约束「≥25% 白底留白、无渐变」；降低场景物件数 |
| 衣服和锚点一起变色 | 提示词已写「场景色不得染进角色」；让模型「只改衣服不改发色项链」 |

---

## 5. 许可

代码与文档 MIT（见 `LICENSE`）。般叶角色归用户自有，可自由用于个人/商业配图。
